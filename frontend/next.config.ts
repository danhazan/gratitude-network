import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep this configuration minimal to avoid conflicts with Next.js's internal CSS handling.
  // All PostCSS and Tailwind CSS configurations have been removed.
};

export default nextConfig;
